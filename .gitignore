# Dependencies
node_modules/

# Claude Code — track skills and commands, ignore local config
.claude/*
!.claude/skills/
!.claude/commands/
.claude/settings.local.json
.claude/node_modules/

# Credentials and keys (NEVER commit)
*.key
*.pem
cosign.key
cosign.pub
tls.key
tls.crt
.env
*.local.json

# Local node addresses / SSH user (copy from nodes.env.example)
nodes.env

# Temporary files
*.bak
*.tmp
*.log

# OS files
.DS_Store
Thumbs.db

# Kubernetes local files
kubeconfig
*.kubeconfig

# Test outputs
kube-bench-master.txt
kube-bench-node.txt
nginx-results.json
sbom-*.json

# Docker/registry temp
Dockerfile.bad
Dockerfile.good
